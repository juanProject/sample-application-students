language: java

services:
  - docker

script:
  - mvn clean verify
  - docker build -t tp_database ./Database

after_success:
  - docker login  -p $DOCKERHUB_PWD -u  $DOCKERHUB_USER
  - docker tag tp_database $DOCKERHUB_USER/database_base:1.1
  - docker push tp_database